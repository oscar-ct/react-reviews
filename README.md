# Reviews UI React App w/ Mock Backend

This project was bootstrapped with [Create React App](https://github.com/facebook/create-react-app).

To run this app, you must run the following commands:

### `npm run server`
### `npm start`
